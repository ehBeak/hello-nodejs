package ch5;

/* 연산자 */
public class Chapter5 {
    /* 연산자 우선순위 */
    // 1. 단항연산자(++|--|+|-|!|~)
    // 2. 산술연산자(*|/|% -> +|-)

    /* 비교 연산자 */
    // 같은 종류끼리 비교 가능(double == float OK)
    // 기본: 값, 참조 : 주소 -> ==|!= -> 모든 타입 사용가능
    // > < >= <= boolean을 제외한 기본 자료형

    /* 논리 연산자 */

    /* 삼항 연산자 */
    int a = 10;
    boolean chk = (a >= 9) ? true : false;

    /* instanceof */

    /* 형 변환 */

}
